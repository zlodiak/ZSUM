
  <% if !current_user?(@user) %>
    <%= render 'shared/thumb_icon' %>
  <% end %>

  <% if !current_user?(@user) %><div class="col-xs-10"><% end %>
    <h2 class="blog-post-title blog-post-title-lite"><%= t(:edit_profile) %></h2>
  <% if !current_user?(@user) %></div><% end %>


<%= render 'form' %>

